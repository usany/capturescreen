# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Settings files
**/.claude/settings.json
**/.claude/settings.local.json

# Codex 배치 디버그 로그 (큰 stdout 파일들, 재현 가능)
**/.codex-imagegen-logs/

# 임시 빌더 산출물
_workspace/imagegen_args.tsv

# OS / 캐시
*.swp
*.log
__pycache__/

# Environment & Secrets
.env
.env.local

# Build output
dist/
node_modules/

# Playwright
test-results/
playwright-report/

# Generated client artifacts (planner 9). Rebuilt by `deno task build`, which the
# Playwright webServer command runs before every suite — so they never need committing.
public/js/app.js
public/styles/tailwind.css
